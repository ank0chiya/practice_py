Prototypeパターン
何時使う？
    新しいおオブジェクトをクローン（複製）でにより生成する
    つまりすでにあるオブジェクトをcopyして使うようなパターン
    pythonでは動的にオブジェクトにアクセスできるため不要

Singltonパターン
何時使う？
    インスタンスが一つ必要で、そのインスタンスにプログラム全体からアクセスしたい場合に使用する
    つまりpythonだとグローバル変数的な考え方になる？
    ex) 登録User名一覧を一番最初に取得してそれ以降は取得しないようにできる
        def get_user_contents(refresh=False):
            contents_list = []
            contents_list = find_elements()
            return contents_list
        
        これを使う

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
構造に関するデザインパターン
複数のオブジェクトからより大きい新しいオブジェクトをどのように構築するか

Adaptorパターン
互換性のないインターフェースを持つ別のクラスを利用したいとき、そのどちらのクラスも変更なしに利用する